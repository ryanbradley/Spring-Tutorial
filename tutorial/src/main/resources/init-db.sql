DROP TABLE products IF EXISTS CASCADE;
CREATE TABLE products (
	ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,
		DESCRIPTION VARCHAR(255),
		PRICE DECIMAL(15, 2));

INSERT INTO USERS (ID, DESCRIPTION, PRICE) values (1, 'Lamp', 5.78);
INSERT INTO USERS (ID, DESCRIPTION, PRICE) values (2, 'Table', 75.29);
INSERT INTO USERS (ID, DESCRIPTION, PRICE) values (3, 'Chair', 22.81);
commit;